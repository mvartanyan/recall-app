{
  "name": "recall",
  "version": "0.1.0",
  "description": "Recall desktop app (Tauri + Rust).",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check:ui": "node --check src/main.js",
    "test": "node --test src/ui-helpers.test.mjs tests/ui-contract.test.mjs",
    "lint": "npm run check:ui"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
