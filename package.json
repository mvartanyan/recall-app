{
  "name": "recall",
  "version": "0.1.0",
  "description": "Recall desktop app (Tauri + Rust).",
  "main": "src/main.js",
  "scripts": {
    "dev": "npm-run-all -p serve tauri:dev",
    "serve": "http-server ./src -p 1420 -c-1",
    "tauri:dev": "tauri dev",
    "build": "tauri build",
    "lint": "echo 'No lint configured yet'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5"
  }
}
